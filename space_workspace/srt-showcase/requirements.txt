# SRT Showcase Space — runtime deps.
# The srt / srt_introspect packages are installed from the GitHub repo so the
# Space tracks the same code as the project.
srt-adapter @ git+https://github.com/space-bacon/SRT.git@main

# HARD PIN: transformers 4.55+ breaks the adapter's manual-decoder generation.
transformers==5.0.0rc3
# torch>=2.6 mitigates CVE-2026-1839 (GHSA-69w3-r845-3855): the Trainer RCE via
# torch.load is neutralised because safe_globals() only protects on torch>=2.6.
# This Space is inference-only and never uses the HF Trainer, so the path is
# also unreachable; the floor is defence-in-depth and keeps the validated
# transformers pin intact.
torch>=2.6
gradio==6.17.3
spaces>=0.30
huggingface_hub>=0.24
accelerate>=0.33
safetensors>=0.4
sentencepiece
numpy
