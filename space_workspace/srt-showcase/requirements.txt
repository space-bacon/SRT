# SRT Showcase Space — runtime deps.
# The srt / srt_introspect packages are installed from the GitHub repo so the
# Space tracks the same code as the project.
srt-adapter @ git+https://github.com/space-bacon/SRT.git@main

# transformers>=5.0.0rc3 carries the CVE-2026-1839 fix (Trainer torch.load RCE)
# and is verified bit-exact with the SRT manual decoder loop: R0 smoke parity
# passes at 0.000e+00 on Qwen2 and Qwen3 dense under transformers 5.7.0. This
# Space is inference-only (Qwen2.5-7B), the exact stack that was verified.
transformers>=5.0.0rc3
torch>=2.6
gradio==6.17.3
spaces>=0.30
huggingface_hub>=0.24
accelerate>=0.33
safetensors>=0.4
sentencepiece
numpy
