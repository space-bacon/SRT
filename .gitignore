__pycache__/
**/__pycache__/
*.pyc
*.egg-info/
dist/

# Built paper PDF lives only in arxiv/; root copy is regenerated by build.sh
/paper.pdf

# C1 corpus (sources, manifests, builder, scored data) is held back from public
# release as the next-gen training pipeline IP. The directory may exist locally
# but is not part of the published repository.
data/corpus_c1/
build/
.venv/
.venv-tools/
*.pt
*.bin
checkpoints/
wandb/
.ruff_cache/
.mypy_cache/
**/.mypy_cache/
.pytest_cache/
release/
.env
.env.*
!.env.example
# top-level local HF working dir; tracked HF release artifacts live in docs/hf/
/hf/

# IDE / OS / local tooling
.DS_Store
**/.DS_Store
**/# Code Citations.md
.vscode/
.idea/

# local artifacts not committed for releases
artifacts/*.log
artifacts/train_log.jsonl

# Private / local-only notes (never pushed)
private/

# Personal correspondence and substack progress notes (held back from public release)
**/*email_draft*
**/*EMAIL_DRAFT*
**/*email_draft*.md
**/*email_draft*.html
**/paper_notification_email*
**/*-email.md
**/*-email.html
erik-email.md
erik-email.html
docs/substack_*
**/*newsletter*
*.eml

# Local scratch / one-shot experiments
/script.py

.venv-adapter/
artifacts/local/

# SugarCrepe working dir: benchmark JSON is fetched, state caches are large (publish to HF instead)
sugarcrepe/*.npz

# Fetched third-party benchmark data and large state caches (published to HF, not git)
sugarcrepe/
artifacts/nla/q4/qwen3b_caches/

# generated: base64-inlined copy of the marketing images, regenerable and
# duplicates every asset beside it
artifacts/marketing/**/post.html
